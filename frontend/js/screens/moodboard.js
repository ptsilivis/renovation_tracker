// Placeholder — implemented in a later phase.
import { h } from '../ui.js';
export default function render(root) {
  root.append(h('div', { class: 'hint', style: { padding: '40px 0' } }, 'moodboard — soon'));
}
