# Copy to backend/.env and fill in. Never commit the real .env.

# SQLAlchemy URL (psycopg v3 driver). Local dev via docker-compose:
DATABASE_URL=postgresql+psycopg://kampos:kampos@localhost:5432/kampos

# JWT signing secret — generate with: python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_HOURS=168

# Set true in production (Cloudflare Tunnel serves HTTPS) so the auth cookie is Secure.
COOKIE_SECURE=false

# Where uploaded files (photos, GLB, receipts) are stored on disk.
UPLOAD_DIR=./uploads
MAX_UPLOAD_MB=50

# Initial password for the 4 seeded family accounts (change after first login).
SEED_PASSWORD=kampos2026
